# Источник контракта

- Репозиторий: `portable-agent/contracts`
- Release: `v2.3.0`
- Файл: `schemas/action-confirmation.schema.json`

Снимок нельзя менять вручную. Новая схема сначала выпускается в `contracts`, затем отдельный pull
request обновляет этот файл, generated type и версию в документации.
